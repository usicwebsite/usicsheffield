import Hero from "@/components/Hero";

/**
 * Landing page component
 * A focused landing page that motivates users to start building with the template
 */
export default function Home() {
  return (
    <div className="min-h-screen bg-gray-900">
      <Hero />
    </div>
  );
}
